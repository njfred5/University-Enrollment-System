{
  "name": "university-enrollment",
  "version": "1.0.0",
  "description": "University Enrollment System — DDD + Branded Types + Observer Pattern",
  "main": "index.ts",
  "scripts": {
    
    "build": "tsc",
    "start": "ts-node index.ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  }
}
